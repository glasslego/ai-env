# MCP 서버 정의
# 이 파일에서 MCP 서버를 정의하면 ai-env가 각 클라이언트용 config를 생성합니다.

mcp_servers:
  # === Docker 기반 서버 ===
  # GitHub.com (glasslego)
  github:
    enabled: true
    type: stdio
    startup_timeout_sec: 45
    command: docker
    args:
      - run
      - -i
      - --rm
      - -e
      - GITHUB_PERSONAL_ACCESS_TOKEN
      - ghcr.io/github/github-mcp-server
      - stdio
    env_keys:
      - GITHUB_GLASSLEGO_TOKEN
    targets:
      - claude_desktop
      - codex_desktop
      - claude_local

  # GitHub Kakao Enterprise
  github-kakao:
    enabled: true
    type: stdio
    startup_timeout_sec: 45
    command: docker
    args:
      - run
      - -i
      - --rm
      - -e
      - GITHUB_PERSONAL_ACCESS_TOKEN
      - -e
      - GITHUB_HOST=https://github.daumkakao.com
      - ghcr.io/github/github-mcp-server
      - stdio
    env_keys:
      - GITHUB_TOKEN
    targets:
      - claude_desktop
      - codex_desktop
      - claude_local

  jira-wiki-mcp:
    enabled: true
    type: stdio
    startup_timeout_sec: 60
    command: docker
    args:
      - run
      - --rm
      - -i
      - -e
      - JIRA_URL=${JIRA_URL}
      - -e
      - JIRA_PERSONAL_TOKEN=${JIRA_TOKEN}
      - -e
      - CONFLUENCE_URL=${WIKI_BASE_URL}
      - -e
      - CONFLUENCE_PERSONAL_TOKEN=${WIKI_TOKEN}
      - ghcr.io/sooperset/mcp-atlassian:latest
    env_keys:
      - JIRA_URL
      - JIRA_TOKEN
      - WIKI_BASE_URL
      - WIKI_TOKEN
    targets:
      - claude_desktop
      - codex_desktop
      - claude_local

  # === NPX 기반 서버 ===
  playwright:
    enabled: true
    type: stdio
    command: npx
    args:
      - -y
      - "@executeautomation/playwright-mcp-server"
    targets:
      - claude_desktop
      - codex_desktop
      - claude_local
      - codex

  desktop-commander:
    enabled: true
    type: stdio
    startup_timeout_sec: 60
    command: npx
    args:
      - -y
      - "@wonderwhy-er/desktop-commander"
    targets:
      - claude_desktop
      - codex_desktop
      - claude_local
      - codex

  brave-search:
    enabled: true
    type: stdio
    command: npx
    args:
      - -y
      - "@modelcontextprotocol/server-brave-search"
    env_keys:
      - BRAVE_API_KEY
    targets:
      - claude_desktop
      - codex_desktop
      - claude_local
      - codex

  context7:
    enabled: true
    type: stdio
    command: npx
    args:
      - -y
      - "@upstash/context7-mcp@latest"
    targets:
      - claude_desktop
      - codex_desktop
      - claude_local
      - codex

  mem0:
    enabled: true
    type: stdio
    startup_timeout_sec: 30
    command: npx
    args:
      - -y
      - "@mem0/mcp"
    env_keys:
      - MEM0_API_KEY
    targets:
      - claude_local

  fetch:
    enabled: true
    type: stdio
    command: uvx
    args:
      - mcp-server-fetch
    targets:
      - claude_desktop
      - codex_desktop
      - claude_local
      - codex

  filesystem:
    enabled: true
    type: stdio
    command: npx
    args:
      - -y
      - "@modelcontextprotocol/server-filesystem"
      - .
    targets:
      - claude_desktop
      - codex_desktop
      - claude_local
      - codex

  git:
    enabled: true
    type: stdio
    command: uvx
    args:
      - mcp-server-git
    targets:
      - claude_desktop
      - codex_desktop
      - claude_local
      - codex

  memory:
    enabled: true
    type: stdio
    command: npx
    args:
      - -y
      - "@modelcontextprotocol/server-memory"
    targets:
      - claude_desktop
      - codex_desktop
      - claude_local
      - codex

  # Supabase hosted MCP endpoint (remote)
  supabase:
    enabled: true
    type: stdio
    startup_timeout_sec: 60
    command: npx
    args:
      - -y
      - mcp-remote
      - https://mcp.supabase.com/mcp
    targets:
      - codex_desktop
      - claude_local
      - codex

  # Browserbase remote MCP endpoint (requires custom remote URL)
  browserbase:
    enabled: false
    type: stdio
    startup_timeout_sec: 60
    command: npx
    args:
      - -y
      - mcp-remote
      - ${BROWSERBASE_MCP_URL}
    env_keys:
      - BROWSERBASE_MCP_URL
    targets:
      - codex_desktop
      - claude_local
      - codex

  sequential-thinking:
    enabled: true
    type: stdio
    command: npx
    args:
      - -y
      - "@modelcontextprotocol/server-sequential-thinking"
    targets:
      - claude_desktop
      - codex_desktop
      - claude_local
      - codex

  # === SSE 기반 서버 ===
  browseract:
    enabled: false
    type: sse
    url_env: BROWSERACT_MCP_URL
    targets:
      - claude_local
      - codex

  # Kakao 내부
  kkoto-mcp:
    enabled: true
    type: sse
    startup_timeout_sec: 30
    url_env: KKOTO_MCP_URL
    targets:
      - codex_desktop
      - claude_local

  cdp-mcp-server:
    enabled: true
    type: sse
    startup_timeout_sec: 30
    url_env: CDP_MCP_URL
    targets:
      - codex_desktop
      - claude_local
